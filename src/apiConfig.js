export function getApiEndpoint() {
    const apiEndpoint = "https://startup-compass-api.onrender.com";
    return apiEndpoint;
  }

export function getAIApiEndpoint() {
    const aiApiEndpoint = "https://business-report-worker.adriane-loquinte.workers.dev";
    return aiApiEndpoint;
  }